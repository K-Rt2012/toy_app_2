<h1>New micropost</h1>

<div>
  <div>
    <%= form_tag("/microposts/create") do %>
      <ul>
        <% @micropost.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>

      <p>
        Content<br>
        <textarea name = "content"></textarea>
      </p>

      <p>
        User<br>
        <select name="user_id" >
        <% @users.each do |user|%>
        <option><%= user.id %></option>
        <% end %>
      </select>

      </p>
      
      <p><input type="submit" value="Create Micropost"></p>
    <% end %>
  </div>

  <p><%= link_to("Back","/microposts") %></p>
</div>